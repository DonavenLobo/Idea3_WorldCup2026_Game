# Card Templates

Each card template should be versioned and treated as an asset plus metadata contract.

Required per template:

```txt
template-key/
  source.fig or source-link.md
  base-card.png
  preview-filled.png
  metadata.json
  notes.md
```

Do not overwrite templates silently. Create a new `-v2` folder when placement, art, or eligibility changes.
