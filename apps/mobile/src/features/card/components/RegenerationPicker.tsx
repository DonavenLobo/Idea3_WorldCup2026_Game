import { EmptyState } from "@world-cup-game/ui";

export function RegenerationPicker() {
  return <EmptyState title="Pick your new card" body="Show up to three generated options after regeneration." />;
}
