import { ScreenPlaceholder } from "../../src/components/layout/ScreenPlaceholder";

export default function JoinGroupScreen() {
  return (
    <ScreenPlaceholder
      title="Join A Group"
      body="Groups are strongly encouraged after card creation, but solo usage remains available."
    />
  );
}
