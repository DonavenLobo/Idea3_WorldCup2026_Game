import { ScreenPlaceholder } from "../../src/components/layout/ScreenPlaceholder";

export default function PhotoBoothScreen() {
  return (
    <ScreenPlaceholder
      title="Photo Booth"
      body="Capture or upload a photo, crop it, then send it to the avatar generation workflow."
    />
  );
}
