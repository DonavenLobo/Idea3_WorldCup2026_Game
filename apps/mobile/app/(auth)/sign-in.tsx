import { ScreenPlaceholder } from "../../src/components/layout/ScreenPlaceholder";

export default function SignInScreen() {
  return (
    <ScreenPlaceholder
      title="Sign In"
      body="Account creation starts after card preview so onboarding can keep momentum."
    />
  );
}
