import { ScreenPlaceholder } from "../../src/components/layout/ScreenPlaceholder";

export default function HomeScreen() {
  return (
    <ScreenPlaceholder
      title="Matchday Home"
      body="Surface card status, today's trivia, live bounties, streaks, and group activity."
    />
  );
}
