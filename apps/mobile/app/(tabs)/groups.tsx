import { ScreenPlaceholder } from "../../src/components/layout/ScreenPlaceholder";

export default function GroupsScreen() {
  return (
    <ScreenPlaceholder
      title="Groups"
      body="Multiple groups, multiple leaderboards, most active leaderboard default."
    />
  );
}
