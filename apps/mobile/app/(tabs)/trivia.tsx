import { ScreenPlaceholder } from "../../src/components/layout/ScreenPlaceholder";

export default function TriviaScreen() {
  return (
    <ScreenPlaceholder
      title="Daily Trivia"
      body="Five fixed questions, four options each, first attempt counts, score by correctness plus speed."
    />
  );
}
