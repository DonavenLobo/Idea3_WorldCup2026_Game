{
  "name": "@world-cup-game/game-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"No tests yet for @world-cup-game/game-engine\"",
    "trivia:generate": "tsx scripts/generateTriviaSeed.ts"
  },
  "dependencies": {
    "@world-cup-game/config": "workspace:*",
    "@world-cup-game/types": "workspace:*"
  }
}
